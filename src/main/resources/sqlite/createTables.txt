DROP TABLE IF EXISTS champions;

CREATE TABLE IF NOT EXISTS champions (
	id TEXT PRIMARY KEY,
	name TEXT NOT NULL,
	title TEXT NOT NULL,
	key INTEGER NOT NULL,
	splash_img_url TEXT NOT NULL,
	loading_img_url TEXT NOT NULL,
	icon_img_url TEXT NOT NULL
);